CREATE TABLE protocol_sequencial (
                                     id BIGINT PRIMARY KEY,
                                     product_code VARCHAR(255),
                                     protocol_year INTEGER,
                                     last_number INTEGER
);
