spring:
  application:
    name: ms-notification-services
  rabbitmq:
    addresses: rabbitmq:5672
    username: guest
    password: guest

server:
  port: 9096

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    prefer-ip-address: true